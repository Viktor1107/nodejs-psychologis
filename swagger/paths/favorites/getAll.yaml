get:
  summary: Get favorite psychologists
  tags: [Favorite]
  security: [bearerAuth: []]
  responses:
    '200':
      description: List of favorites
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/favorite.yaml

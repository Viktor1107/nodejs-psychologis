delete:
  summary: Delete avatar
  tags: [Auth]
  security: [bearerAuth: []]
  responses:
    '200':
      description: Avatar deleted
      content:
        application/json:
          schema:
            type: object
            properties:
              message: { type: string, example: 'Avatar deleted' }
